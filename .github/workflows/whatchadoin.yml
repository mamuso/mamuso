name: whatchadoin

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  peeking:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run a one-line script
      run: echo Hello, world!
